# ECLIPSE Event Catalog
*Governance: GDD 14.2 — every event-bus tag is documented here in the same commit that adds it. GDD 12.2 rule 2 — subsystems communicate only via these events (or explicit public interfaces).*

**Status legend:** `specified` = named in a Phase 1 spec, no code yet · `declared` = native tag exists in `Source/Eclipse/Core/EclipseGameplayTags.cpp` (SPEC-P1-01 bus infrastructure), emitter pending · `implemented` = native tag + emitter exist.

Payload structs live in `Core/EclipseEventPayloads.h`. Rows move to `implemented` in the commit that adds their emitter (specs P1-02…08).

| Tag | Payload (struct) | Emitted by | Consumed by | Spec | Status |
|---|---|---|---|---|---|
| `Event.Campaign.DayAdvanced` | `FEclipseCampaignEventPayload` (new day index) | CampaignSubsystem (commit) | Economy (tick) | P1-02 | implemented |
| `Event.Economy.ResourcesChanged` | `FEclipseEconomyEventPayload` (type, delta, new balance, reason) | CampaignSubsystem (commit) | Mini-map UI, Base UI | P1-02/03 | implemented |
| `Event.Economy.ProductionQueued` | `FEclipseEconomyEventPayload` (item id, eta) | CampaignSubsystem (commit) | Base UI | P1-03 | implemented |
| `Event.Economy.ProductionCompleted` | `FEclipseEconomyEventPayload` (item id, loadout tag) | Economy tick → commit | Base UI, Preparation | P1-03 | declared |
| `Event.Strategy.RegionControlChanged` | `FEclipseStrategyEventPayload` (region id, old/new owner) | CampaignSubsystem (commit) | Mini-map UI. A liberation instance emits this **once per flipped region** out of its single transaction — the trio's fact stream, which is why SPEC-P2-05 dropped `LiberationPhaseAdvanced` as a P2-00 amendment | P1-02/04/P2-05 | implemented |
| `Event.Strategy.MissionSelected` | `FEclipseStrategyEventPayload` (region id, template id) | StrategySubsystem (map screen pick) | Preparation flow | P1-04 | implemented |
| `Event.Strategy.LiberationResolved` | `FEclipseLiberationEventPayload` (row name, flipped region count, authored context line) | StrategySubsystem, **after** the liberation transaction commits — so after the per-region facts above | Base hub debrief (the one line that says *why*, not *what*) | P2-05 | implemented |
| `Event.Strategy.ResponseTierChanged` | `FEclipseResponseTierEventPayload` (old tier, new tier, day, reason) | CampaignSubsystem (commit: SetResponseTier mutation) — and the only writer that raises it today is `UEclipseStrategySubsystem::OnMissionCompleted`, which re-derives the tier from the campaign's own facts *after* a liberation has landed (`EclipseStrategyLogic::DeriveResponseTier`) | Routing (`EclipseStrategyLogic` adds `LaneTuning.RiskPerResponseTier` to every leg — the tier is not a label, it changes which route is cheapest and which is refusable), `Eclipse.Strategy.Board` console readout, scenario tests. Planned: the diegetic broadcast GDD 9.4 asks for (propaganda, patrol chatter) and the map screen | GDD 9.4 | implemented |
| `Event.Prep.MissionLaunchRequested` | `FEclipsePrepEventPayload` (mission id, squad ids, loadout, insertion, intel level) | PrepSubsystem (validated launch) | Mission runtime | P1-08 | implemented |
| `Event.Mission.Started` | `FEclipseMissionEventPayload` (mission id) | MissionSubsystem | Squad (spawn), UI; resets the Command Mode usage tally (per-run telemetry) | P1-05 | implemented |
| `Event.Mission.ObjectiveCompleted` | `FEclipseMissionEventPayload` (mission id, objective id) | MissionSubsystem (objective components report in) | Mission phases, UI | P1-05 | implemented |
| `Event.Mission.Completed` | `FEclipseMissionEventPayload` (mission id, results) | MissionSubsystem (debrief) | Economy, Roster, Base UI, audio sting (16.12), **liberation trigger** (`UEclipseStrategySubsystem::OnMissionCompleted` → `EclipseLiberationLogic`: resolves `DT_LiberationInstances` and commits one SetRegionOwner transaction — the beat it gates on is committed *before* this broadcast, see SPEC-P2-05) | P1-05/P2-05 | implemented |
| `Event.Mission.Failed` | `FEclipseMissionEventPayload` (mission id, results) | MissionSubsystem (debrief) | Economy, Roster, Base UI | P1-05 | implemented |
| `Event.Mission.PhaseChanged` | `FEclipseMissionEventPayload` (mission id, phase name, authored-sub-phase flag) | MissionSubsystem: the four outer transitions (Insertion/Objectives/Extraction/Debrief) with `bAuthoredSubPhase=false`; `NotifyAlarmRaised()` broadcasts the `"Alarm"` sub-phase with `true`, once per run (idempotent). Authored StateTree sub-phases land later on the same tag. | Scenario tests only, today. **Alarm has no event of its own** — consumers key on `PhaseName == "Alarm"` (SPEC-P2-04 resolved point 2). `UEclipseCommandModeComponent` keys on `PhaseName == "Alarm"` to close the running encounter beat (R3 criterion 5 segmentation). Planned: mission HUD proper, 16.7 audio layers. | P2-04 | implemented |
| `Event.Squad.OrderIssued` | `FEclipseSquadEventPayload` (soldier id, order, target) | SquadSubsystem (order dispatch) | Squad AI, debug UI. **Not** the round-trip meter: that lives inside `UEclipseSquadSubsystem::IssueOrder` itself, where both halves of the trip are visible, and it stamps `FPlatformTime` — never world time, because Command Mode dilates to 0.30 (R3 criterion 1). The HUD only reads the tally. | P1-06/P2-01 | implemented |
| `Event.Squad.OrderAcknowledged` | `FEclipseSquadEventPayload` (soldier id, order, bark line) | SquadSubsystem (after decision) | Debug UI, (later) VO; the round-trip sample is recorded right after this broadcast — the criterion is "the tester hears an answer" | P1-06/P2-01 | implemented |
| `Event.Combat.HitLanded` | `FEclipseCombatEventPayload` (plek van de treffer, kopschot ja/nee, schade) | HitscanWeaponComponent (alleen bij een treffer) | AudioSubsystem: inslaggeluid op de trefferplek; de hitmarker gaat hier straks ook aan hangen | 26-07 | implemented |
| `Event.Combat.WorldImpact` | `FEclipseCombatEventPayload` (schutter, plek van de inslag) | HitscanWeaponComponent (alleen als het GEEN personage was) | AudioSubsystem: hetzelfde inslaggeluid, zodat een MISSER ook hoorbaar is | 27-07 | implemented |
| `Event.Combat.ShotFired` | `FEclipseCombatEventPayload` (schutter, oorsprong, geluidsradius) | HitscanWeaponComponent (elk schot dat de cadans passeert, raak of mis) | GameMode: alarmeert vijanden binnen de radius en stuurt ze naar de oorsprong als laatst bekende positie | 26-07 punt 1 | implemented |
| `Event.Combat.ReloadStarted` | `FEclipseCombatEventPayload` (schutter, wapenfamilie, duur) | HitscanWeaponComponent (bij het begin van een herlaadbeurt) | AudioSubsystem: de foley-keten — magazijn pakken, laten vallen, insteken, grendel — verdeeld over de duur | 26-07 avond | implemented |
| `Event.Combat.WeaponSwapped` | `FEclipseCombatEventPayload` (schutter, nieuwe wapenfamilie, opheftijd) | HitscanWeaponComponent (bij een geslaagde wissel) | AudioSubsystem: het equip-geluid van de familie die je optilt | 26-07 avond | implemented |
| `Event.Squad.OrderRefused` | `FEclipseSquadEventPayload` (soldier id, order, reason) | SquadSubsystem (after decision) | Debug UI, scenario tests; counts as an answer for the round-trip meter (a refusal is a response, not a timeout) | P1-06/P2-01 | implemented |
| `Event.Squad.SoldierDowned` | `FEclipseSquadEventPayload` (soldier id, cause) | SquadSubsystem (body OnDowned) | Mission runtime (debrief resolution); auto-triage dispatch runs in-subsystem off the same down, not via a bus subscription | P1-06/07/P2-01 | implemented |
| `Event.Squad.SoldierStabilized` | `FEclipseSquadEventPayload` (soldier id, stabilizer id, bark line) | SquadSubsystem (triage arrival inside the window) | Mission runtime (casualty resolution), debrief UI | P2-01 | implemented |
| `Event.Squad.ClassAbilityUsed` | `FEclipseSquadEventPayload` (soldier id, ability verb tag) | SquadSubsystem (signature verb fired) | Debug UI, (later) VO/XP | P2-01 | implemented |
| `Event.Squad.OrderQueued` | `FEclipseSquadEventPayload` — struct **unchanged**; the `Order` field carries the *transition* (`Flank.Proposed` / `Flank.Approved` / `Flank.Expired` / `Flank.Cancelled` / `SyncStrike.Marked` / `SyncStrike.Unmarked` / `SyncStrike.Pruned` / `Breach.Stacked` / `Breach.Entry`), `TargetId` the marked actor, `BarkLine` the spoken line where there is one | `UEclipseSquadSubsystem::BroadcastOrderQueued` — the **only** emitter, same as for the Issued/Acknowledged/Refused trio. The squadmate controller reports its flank transitions *through* the subsystem instead of broadcasting, so "one order path" stays literally true (`Eclipse.Command.StageB.OrderEventsHaveOneEmitter` goes red on a second emitter) | Command debug UI (route preview, mark pips — draws *and clears* from this one stream), scenario tests. **Negative transitions ride here too**: an expired flank window and a pruned mark are facts, not refusals, and they are never silent (GDD 9.5) | P2-02 | implemented |
| `Event.Squad.SelfAction` | `FEclipseSquadEventPayload` (soldier id, Reason: Contact/TakingFire/Reloading) | SquadmateController (handelt uit zichzelf) | Audiolaag (stem, met 2 s rem per soldaat) | owner 27-07 | implemented |
| `Event.Player.VitalsChanged` | `FEclipsePlayerVitalsPayload` (health + max + previous health, stance + previous stance, downed, and three flags saying *which* of the three moved; `bInitial` marks the first picture on possession) | `AEclipseCharacter`, but **only for the player-controlled body** and only through `EclipseVitalsFeed::FEclipseVitalsTracker` — that tracker is the thing that guarantees "no change = no broadcast". Emit sites: `NotifyControllerChanged` (initial), `HandleHealthChanged`, `InitializeHealth`/`ReviveForMission`, `OnStartCrouch`/`OnEndCrouch`, `SetSprinting` (driven from `ApplySprintDrive`). A squadmate or enemy body never emits here — their facts are `Event.Squad.*`, which carry a soldier id and feed the debrief. | Mission HUD (health bar, stance readout, downed state). Replaces per-frame polling of the pawn: the widget owns no copy of the previous value, the payload carries it | HUD spoor-B / GDD 12.2 rule 2 | implemented |
| `Event.Command.ModeEntered` | `FEclipseCommandEventPayload` (dilation factor) | CommandModeComponent (hold began) | Audio duck/filter (P2-09), debug UI now / UI stack (P2-07), M1.1 teaching beat (P2-04), scenario tests, and the encounter-beat usage tally in `UEclipseCommandModeComponent` itself (R3 criterion 5) | P2-02 | implemented |
| `Event.Command.ModeExited` | `FEclipseCommandEventPayload` (held seconds, orders issued while held) | CommandModeComponent (release or fail-safe) | Same consumers as ModeEntered; feel-gauntlet telemetry | P2-02 | implemented |
| `Event.Base.ConstructionStarted` | `FEclipseBaseEventPayload` (slot id, facility id, target level, ETA day) | CampaignSubsystem (commit: build order accepted) | Vault presentation (`UEclipseBaseSubsystem` → `EclipseVaultBuilder`, one family-level subscription, re-render coalesced on plan-hash: one rebuild per commit — scaffold state on), base/debug UI, audio | P2-03 | implemented |
| `Event.Base.FacilityBuilt` | `FEclipseBaseEventPayload` (slot id, facility id, level = 1) | CampaignSubsystem (commit: construction-tick completion, or the rush commit itself) | Vault presentation (same coalesced re-render path: state swap + global growth tier), UI, audio, soak asserts | P2-03 | implemented |
| `Event.Base.FacilityUpgraded` | `FEclipseBaseEventPayload` (slot id, facility id, new level) | CampaignSubsystem (commit) | Same as FacilityBuilt | P2-03 | implemented |
| `Event.Base.StaffAssigned` | `FEclipseBaseEventPayload` (slot id, facility id, soldier id, role tag: crew/analyst/empty = unassigned incl. completion release and the casualty release from KillSoldier/WoundSoldier) | CampaignSubsystem (commit) | Muster/roster UI (soldier unavailable), vault presentation (same coalesced re-render path: crew idlers) | P2-03 | implemented |
| `Event.Story.BeatReached` | `FEclipseStoryEventPayload` (beat tag, day) | CampaignSubsystem (commit: SetStoryFlag mutation) | Story pinned-offer refresh (map), UI (P2-07), (later) beat-sting audio (16.12), soak asserts | P2-04 | implemented |
| `Event.Roster.SoldierAdded` | `FEclipseRosterEventPayload` (soldier record) | CampaignSubsystem (commit) | Barracks UI | P1-07 | implemented |
| `Event.Roster.SoldierDied` | `FEclipseRosterEventPayload` (soldier id, cause, day) | CampaignSubsystem (commit) | Barracks UI, Memorial | P1-07 | implemented |
| `Event.Roster.SoldierWounded` | `FEclipseRosterEventPayload` (soldier id, days out) | CampaignSubsystem (commit) | Barracks UI | P1-07 | implemented |
| `Event.Memorial.EntryAdded` | `FEclipseRosterEventPayload` (memorial entry) | CampaignSubsystem (commit) | Memorial UI | P1-07 | implemented |

**Rules for adding a tag:** name follows `Event.<System>.<PastTenseFact>`; facts, not commands (the bus reports what happened; the transaction API is how you make things happen — GDD 14.3.3). State-changing facts are emitted only by the CampaignState commit, never directly by gameplay code.
